# List of required packages
required_packages <- c(
  "pacman"
)

# Load the required packages
pacman::p_load(forecast)
pacman::p_load(reset_test)
pacman::p_load(ggplot2)
pacman::p_load(dplyr)
pacman::p_load(lubridate)
pacman::p_load(tseries)
